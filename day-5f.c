#include <stdio.h>


int main() {
    double x, y, result;


    printf("Enter value for x: ");
    scanf("%lf", &x);
    printf("Enter value for y: ");
    scanf("%lf", &y);

    // Calculate (x + y)3
    result = (x + y, 3);


    printf("The result of (%lf + %lf)3 is: %lf\n", x, y, result);

 
}